import React from 'react';

// Components
import Layout from './layouts/default';
import Result from './components/result';

export default () => {
    return (
        <Layout>
            <Result />
        </Layout>
    );
};
